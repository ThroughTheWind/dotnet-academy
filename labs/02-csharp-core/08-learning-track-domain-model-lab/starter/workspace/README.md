# Workspace Guidance

Create the learner lab project in this folder so the starter assets and your code stay together.

Suggested commands:

```bash
dotnet new console -n LearningTrackPlannerConsole
dotnet run --project ./LearningTrackPlannerConsole
```

After the first version runs, compare your structure with `../expected-output.md`.