# Micro Exercise

Take 10 minutes and answer this question without building the full exercise first.

You are given a sample project with these properties:

- `OutputType` = `Exe`
- `PackageId` = `DotnetAcademy.DiagnosticsRunner`
- `Version` = `1.0.0`
- `IsPackable` = `false`

Write two or three sentences that explain:

1. whether `dotnet pack` should be part of the default release flow
2. which property most clearly communicates that decision
3. which command should still run before release to prove the executable is healthy

Then compare your answer with the rules in `../lesson.md` and the full expected output example.