# Workspace

Use this folder as the working directory for the exercise.

Suggested command sequence:

```powershell
dotnet new console -n LearnerCardConsole
cd LearnerCardConsole
dotnet run
dotnet run -- Ada
```

After the project works, compare your output with `expected-output.md`.