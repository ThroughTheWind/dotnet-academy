# Workspace

Use this folder as the working directory for the exercise.

Recommended commands:

```powershell
dotnet --version
dotnet --list-sdks
dotnet new console -n HelloDotnetAcademy
cd HelloDotnetAcademy
dotnet run
```

After you finish, the generated project can remain here as your own practice artifact.