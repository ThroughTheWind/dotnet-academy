# Workspace

Use this folder as the working directory for the exercise.

Suggested command sequence:

```powershell
dotnet new console -n LearnerProgressCard
cd LearnerProgressCard
dotnet run
```

After the project works, compare your result with `expected-output.md`.