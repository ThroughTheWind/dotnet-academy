# Challenge Brief

Build a console app named `PlanningBoardConsole` that manages a small study planning board.

Your app must:

- keep ordered items in a mutable collection
- support lookup by item code
- track unique categories
- publish a stable report snapshot
- prove that the snapshot does not change after a later update to the live board

Keep the design small enough that another learner can explain each collection choice in one sentence.