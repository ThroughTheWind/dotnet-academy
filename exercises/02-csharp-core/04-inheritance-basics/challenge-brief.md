# Challenge Brief

Build a console app named `ResourcePlannerConsole` that models two kinds of learning resources through a shared base type.

Your app must:

- define an abstract base class
- create at least two derived resource classes
- override behavior in one derived class
- print summaries through base-type references

Keep the hierarchy small and explain why the derived types are true specializations of the base type.