# Challenge Brief

Build a console app named `StudyGroupConsole` that models a small study group.

Your app must:

- define one class that represents a group member
- create at least two objects from that class
- store each member's progress as object state
- use a method on the class to produce readable output

Keep the model small and readable. The first goal is to understand classes and objects, not to build a large system.