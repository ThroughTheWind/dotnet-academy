# Challenge Brief

Build a console app named `SessionSignalConsole` that records learner session results, raises a notification event, and prints only the results that still need follow-up.

Your app must:

- define a small result model
- use a delegate plus a lambda for filtering or formatting
- raise one event per recorded result
- use pattern matching to describe each result clearly

Keep the flow small enough that each behavior choice stays easy to trace.