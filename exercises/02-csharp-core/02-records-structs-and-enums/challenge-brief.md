# Challenge Brief

Build a console app named `MilestoneTrackerConsole` that models a small milestone tracker.

Your app must:

- define one enum for milestone state
- define one record for milestone data
- define one struct for a compact practice budget
- create an updated milestone by copying the original record with one changed value

Keep the exercise focused on choosing the right type for the right kind of data.