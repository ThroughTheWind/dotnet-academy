# Challenge Brief

Build a console app named `LearningPlanConsole` that models a small learning plan.

Your app must:

- define an interface for one learning step
- implement it in at least two different classes
- compose those step objects inside a plan class
- print the resulting agenda and total time

Keep the interface small and the plan focused on coordination instead of detailed step behavior.